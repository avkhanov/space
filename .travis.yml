addons:
   apt:
       packages:
           - cmake
           - libgl1-mesa-dev
           - libxrandr-dev
           - libxinerama-dev
           - libxcursor-dev
sudo: required
language: cpp
script: cmake . && make

